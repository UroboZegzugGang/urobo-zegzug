import SwiftUI

extension ButtonStyle where Self == MonochromeShadowButton {
    static var monochromeShadow: Self {
        Self()
    }
}

extension ButtonStyle where Self == CircularButton {
    static var circular: Self {
        Self()
    }
}

extension ButtonStyle where Self == ScaleOnTap {
    static var scaleOnTap: Self {
        Self()
    }
}
